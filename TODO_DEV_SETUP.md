## Setup for IDE

- [ ] setup auto format code
- [ ] setup auto format imports
- [ ] setup max length lines
- [x] setup ipdb as main debugger (uv add --group=test ipdb && PYTHONBREAKPOINT=ipdb.set_trace)
